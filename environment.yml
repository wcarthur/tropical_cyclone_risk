name: tc_risk
channels:
  - defaults
dependencies:
  - pip
  - numpy
  - basemap
  - cartopy
  - netcdf4
  - dask
  - matplotlib
  - scipy
  - xarray
  - metpy
  - cftime
  - python
  - pip:
    - cdsapi
    - global-land-mask
